name: Deploy Company Profile CMS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd ${{ secrets.PROJECT_PATH }}
            git pull origin main
            echo "${{ secrets.ENV_FILE }}" > .env
            echo "${{ secrets.BACKEND_ENV }}" > backend/.env
            echo "${{ secrets.FRONTEND_ENV }}" > frontend/.env
            docker compose down
            docker compose up -d --build
            docker exec cms_backend composer install --no-interaction --prefer-dist --optimize-autoloader
            docker exec cms_backend php artisan migrate --force
            docker exec cms_backend php artisan storage:link --force
            docker exec cms_backend php artisan optimize:clear


